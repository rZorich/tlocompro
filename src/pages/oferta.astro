---
import BaseLayout from "../layouts/BaseLayout.astro";

const title = "Vende tu auto rápido en Chile | Tasación inmediata en 24 horas | Tlocompro";
const description = "Completa el formulario para recibir una oferta inmediata por tu vehículo usado. Pago al instante, firma en notaría y retiro en 24 horas en cualquier comuna de Chile.";
const canonical = "https://tlocompro.cl/oferta";
---
<BaseLayout {title} {description} {canonical}>
  <section class="max-w-5xl mx-auto px-4 py-8">
    <header class="mb-6">
      <h1 class="text-3xl md:text-4xl font-extrabold tracking-tight">Recibe tu oferta inmediata</h1>
      <p class="mt-2 text-gray-700">En <strong>Tlocompro</strong> compramos autos usados bajo el precio de mercado para ofrecerte <strong>liquidez inmediata</strong>.</p>
    </header>

    <form action="https://script.google.com/macros/s/AKfycbyDiJbUxoCUTXvK8R59CmNyO-GKrcgh0-2tVXk-KzIGR2opVD20bsr7ugL45TnX3gy8vA/exec" method="POST" class="space-y-8">
      <!-- Campo honeypot anti-spam -->
      <input type="text" name="_honey" style="display:none" tabindex="-1" autocomplete="off" />

      <!-- Datos del vehículo -->
      <section class="bg-white border rounded-2xl shadow-card p-6">
        <h3 class="text-lg font-bold mb-4">1) Datos del vehículo</h3>
        <div class="grid md:grid-cols-4 gap-4">
          <input name="marca" type="text" placeholder="Marca" required class="border p-3 rounded-xl" />
          <input name="modelo" type="text" placeholder="Modelo" required class="border p-3 rounded-xl" />
          <input name="anio" type="number" placeholder="Año" required class="border p-3 rounded-xl" />
          <input name="version" type="text" placeholder="Versión" class="border p-3 rounded-xl" />
        </div>
        <div class="grid md:grid-cols-4 gap-4 mt-4">
          <select name="transmision" required class="border p-3 rounded-xl">
            <option value="">Transmisión</option>
            <option>Manual</option>
            <option>Automática</option>
          </select>
          <select name="combustible" required class="border p-3 rounded-xl">
            <option value="">Combustible</option>
            <option>Bencina</option>
            <option>Diésel</option>
            <option>Híbrido</option>
            <option>Eléctrico</option>
          </select>
          <input name="kilometraje" type="number" placeholder="Kilometraje" required class="border p-3 rounded-xl" />
          <input name="patente" type="text" placeholder="Patente" required class="border p-3 rounded-xl" />
        </div>
      </section>

      <!-- Datos personales -->
      <section class="bg-white border rounded-2xl shadow-card p-6">
        <h3 class="text-lg font-bold mb-4">2) Datos de contacto</h3>
        <div class="grid md:grid-cols-3 gap-4">
          <input name="nombre" type="text" placeholder="Nombre y apellido" required class="border p-3 rounded-xl" />
          <input name="whatsapp" type="tel" placeholder="WhatsApp" required class="border p-3 rounded-xl" />
          <input name="email" type="email" placeholder="Correo electrónico" required class="border p-3 rounded-xl" />
        </div>
        <div class="grid md:grid-cols-2 gap-4 mt-4">
          <input name="ciudad" type="text" placeholder="Ciudad" required class="border p-3 rounded-xl" />
          <select name="cuandovender" required class="border p-3 rounded-xl">
            <option value="">Cuándo vender</option>
            <option>Hoy</option>
            <option>1-3 días</option>
            <option>Esta semana</option>
            <option>Solo cotizando</option>
          </select>
        </div>
        <div class="mt-4 text-xs text-gray-600">
          Al enviar aceptas que la oferta será bajo mercado, a cambio de liquidez inmediata.
        </div>
      </section>

      <button type="submit" class="bg-teal-600 hover:bg-teal-700 text-white font-semibold px-6 py-3 rounded-xl shadow-soft">
        💸 Enviar y recibir oferta
      </button>
    </form>
  </section>
</BaseLayout>


